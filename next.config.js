// This file is automatically loaded by Next.js and is used to define custom configuration for your app.
/** @type {import('next').NextConfig} */
const nextConfig = {
    // Target  images on remote servers
    // images: {
    //     remotePatterns: [
    //         {
    //             protocol: 'https',
    //             hostname: 'your-remote-server.com' // name of website take w , h in image 
    //         }
    //     ]
    // }
}

module.exports = nextConfig
